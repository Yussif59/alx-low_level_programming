#include 'main.h'
/**
 * _isolower - Shows 1 if the input is a
 * lowercase character. Another cases, shows
 * 0
 *
 * @c: The character in ASCII code
 *
 * REturn: 1 for lowercase character. 0 for the rest.
 */
int -islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (1);
	}
	_putchar('\n');
}
